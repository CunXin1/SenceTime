# ============================================================================
#  Week3 Day16：把最优 Llama LoRA adapter 合并进基座，归档为下周 DPO 的输入模型。
#  Week3 Day16: merge the best Llama LoRA adapter into the base and archive it.
#  运行 / Run（Windows 用 python -m，勿用 llamafactory-cli.exe——Week2 Day10 FAQ 段错误坑）:
#    .venv/Scripts/python.exe -m llamafactory.cli export Week3/configs/merge_best_llama.yaml
# ============================================================================

### 被合并的基座 + adapter / base + adapter to merge
model_name_or_path: models/Llama-3.2-3B-Instruct
# ★ 待定：实验结束后按《实验结果汇总.md》的最优组更新此路径。
# ★ TBD: update to the winning run per 实验结果汇总.md after the sweep finishes.
adapter_name_or_path: saves/week3/llama/r8_lr1e-4_ep3
template: llama3                   # Llama-3 模板，必须与训练一致 / must match training
finetuning_type: lora
trust_remote_code: true

### 导出 / export
export_dir: models/Llama-3.2-3B-week3-best-merged
export_size: 5
export_device: cpu
export_legacy_format: false
