# Week3 Day16 交付：最优模型归档说明

> 本文档是第 4 周 DPO 工作的入口：说明最优模型是谁、为什么是它、放在哪里。

## 最优配置

| 项 | 值 |
|---|---|
| 基座 | Qwen2.5-3B-Instruct |
| 最优超参组合 | **r=32（α=64）, lr=1e-4, epochs=3**（`r32_lr1e-4_ep3`，final eval loss 1.2630，4 组中最低） |
| 判定依据 | ①实验结果汇总.md（eval loss）②盲测得分汇总.md（加权总分）③OpenCompass评测分数表.md（通用能力底线） |

## 归档路径

| 产物 | 路径 | 说明 |
|---|---|---|
| LoRA adapter（原始产物） | `saves/week3/qwen/r32_lr1e-4_ep3/` | 含训练日志，gitignored |
| **合并后完整模型（DPO 输入）** | `models/Qwen2.5-3B-week3-best-merged/` | safetensors 分片 ≤5GB |
| 合并配置 | `Week3/configs/merge_best_qwen.yaml` | adapter 路径已更新为最优组 |

合并命令（Windows 必须用 python -m，见 Week2 Day10 FAQ）：

```powershell
.\.venv\Scripts\python.exe -m llamafactory.cli export Week3/configs/merge_best_qwen.yaml
```

## 验证记录

- ⏳ 合并后用 `Week2/code/compare_finetune.py --base models/Qwen2.5-3B-Instruct
  --merged models/Qwen2.5-3B-week3-best-merged` 抽查 3 题，确认行为与 adapter 版一致。

## 下周 DPO 衔接

- DPO 的 policy 模型 = `models/Qwen2.5-3B-week3-best-merged`（SFT 后的模型做偏好对齐起点）。
- LLaMA-Factory DPO 配置要点：`stage: dpo`、`model_name_or_path` 指向上面的合并模型、
  需准备偏好数据集（chosen/rejected 对），格式参考 `LLaMA-Factory/data/dpo_zh_demo.json`。
- 本周实验的最优训练超参（学习率/秩）可作为 DPO 的 LoRA 起点参考，但 DPO 学习率
  通常再低一个量级（5e-6 ~ 5e-7 区间起步）。
